
use blog::Post;

fn main() {
    let mut post = Post::new();

    post.add_text("I ate a salad for lunch today");

    let post = post.request_review();

    let post = post.approve();

    assert_eq!("I ate a salad for lunch today", post.content());
}

// rust can do oop but encoding state into type system is also alternative and have different trade-offs
// take advantage of rust features like ownership